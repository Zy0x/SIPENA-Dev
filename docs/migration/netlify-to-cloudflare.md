# Netlify to Cloudflare

1. Pastikan build command `npm run build`.
2. Set output `apps/frontend/dist`.
3. Salin env dari Netlify ke Cloudflare Pages.
4. Pastikan SPA fallback aktif memakai `_redirects`.
5. Jalankan smoke test login, dashboard, presensi, nilai, dan export.
