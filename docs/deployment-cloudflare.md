# Deployment Cloudflare Pages

Build command:

```bash
npm run build
```

Output directory:

```text
apps/frontend/dist
```

Gunakan `_redirects` atau routing fallback platform agar SPA route tidak 404.
