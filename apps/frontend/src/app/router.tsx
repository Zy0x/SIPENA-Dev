import App from "@/app/App";

// Router masih berada di App.tsx untuk menjaga behavior existing.
// Pindahkan route baru ke file ini saat route mulai dipisah per feature.
export const AppRouter = App;
