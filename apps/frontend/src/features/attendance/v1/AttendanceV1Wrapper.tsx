import React from "react";
import Attendance from "@/pages/Attendance";

/**
 * AttendanceV1Wrapper
 * Wraps the legacy Attendance page component. Renders it completely unchanged.
 */
export const AttendanceV1Wrapper: React.FC = () => {
  return <Attendance />;
};

export default AttendanceV1Wrapper;
