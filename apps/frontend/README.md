# SIPENA Frontend

Frontend React + Vite berada di folder ini. Jalankan dari root:

```bash
npm run dev
npm run build
```

Provider runtime dipilih dari variable `VITE_*_PROVIDER` dan dibuat melalui `src/infrastructure/provider.factory.ts`.
